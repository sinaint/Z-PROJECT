# =====================================================
# ⚠️ 절대 커밋하면 안 되는 파일들 (AWS 키, 비밀번호 등)
# =====================================================
.env
.env.local
.env.*.local
*.pem
*.key
aws-credentials.json

# =====================================================
# Terraform (AWS 인프라 코드)
# 주의: .tfstate 파일에는 IP, DB 비밀번호 등 민감정보가
# 평문으로 저장될 수 있어서 절대 올리면 안 돼요!
# =====================================================
*.tfstate
*.tfstate.backup
*.tfstate.*.backup
.terraform/
.terraform.lock.hcl
crash.log

# =====================================================
# Python (Django 백엔드)
# =====================================================
__pycache__/
*.py[cod]
venv/
.venv/
env/
*.egg-info/

# =====================================================
# Node / React (프론트엔드)
# =====================================================
node_modules/
dist/
build/
.npm/

# =====================================================
# 에디터 / OS 관련 (사람마다 환경이 달라서 제외)
# =====================================================
.vscode/
.DS_Store
Thumbs.db

# =====================================================
# 로그 파일
# =====================================================
*.log
